
pub struct Keys {
    pub api_key: String,
    pub secret_key: String
}

pub struct BaseUrl {
    pub socket_spot: String,
    pub socket_perpetual: String,
    pub socket_inverse: String,
    pub socket_option: String,
    pub socket_private: String,
}
